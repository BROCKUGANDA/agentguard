; npm resilience — container builds hit flaky registry connections
; (ECONNRESET mid-download); retry aggressively instead of failing the build.
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000

; Slightly quieter installs
audit=false
fund=false